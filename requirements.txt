fastapi==0.141.1
# Security floor 1.3.1 — closes the 2026-05/06 Starlette advisories:
#   GHSA-86qp-5c8j-p5mr / CVE-2026-48710 — Host header poisons request.url.path (Moderate; fixed 1.0.1)
#   GHSA-wqp7-x3pw-xc5r — StaticFiles UNC-path SSRF / NTLM theft on Windows (High; fixed 1.1.0)
#   GHSA-x746-7m8f-x49c — HTTPEndpoint method dispatched via getattr (Moderate; fixed 1.1.0)
#   GHSA-82w8-qh3p-5jfq / CVE-2026-54283 — request.form() urlencoded DoS (High, CVSS 7.5; fixed 1.3.1)
# Also covers CVE-2025-62727 (FileResponse Range-header DoS) and CVE-2025-54121.
starlette>=1.3.1
uvicorn==0.52.1
httpx==0.28.1
# 2.13.0 closes the five 2026-06 PyJWT advisories:
#   GHSA-xgmm-8j9v-c9wx / CVE-2026-48526 — JWK JSON accepted as HMAC secret / algorithm
#     confusion (High, CVSS 7.4) — not reachable: verify_jwt() pins a single symmetric HS256
#   GHSA-jq35-7prp-9v3f / CVE-2026-48523 — PyJWK alg allow-list bypass (Moderate)
#   GHSA-w7vc-732c-9m39 / CVE-2026-48525 — detached-JWS b64=false DoS (Moderate)
#   GHSA-993g-76c3-p5m4 / CVE-2026-48522 — PyJWKClient non-http(s) URI SSRF (Moderate)
#   GHSA-fhv5-28vv-h8m8 / CVE-2026-48524 — PyJWKClient cache wiped on fetch error (Low)
# (qBitRead uses no PyJWK / PyJWKClient / detached JWS — bumped defensively.)
# Also covers CVE-2026-32597 (crit header not validated, High).
PyJWT==2.13.0
bcrypt==5.0.0
aiosqlite==0.22.1
# 2.14.2+ closes GHSA-4xgf-cpjx-pc3j — NestedSecretsSettingsSource symlink-follow
# bypass of secrets_dir_max_size (Moderate, CVSS 5.3). Not reachable: app/config.py
# uses BaseSettings + env_file only, never the nested-secrets source.
pydantic-settings==2.15.0
